
<%= render 'shared/blog_featured' if !params[:page] || params[:page] == "1" %>

<div class="col-md-8 blog-main">
  <%= link_to "Write a New Blog", new_blog_path if logged_in?(:site_admin) %>

  <div class="border-bottom mt-4">
    <%= will_paginate @blogs, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
  </div>
    
    <%= render @blogs %>

    <%= will_paginate @blogs, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
        
</div>








